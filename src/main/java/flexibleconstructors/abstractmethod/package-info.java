/**
 * This package contains example in case if a parent class has abstract method which might be used for some reason in its constructor.
 * The flexible constructor bodies allow us to mitigate problem, when something is "not fully initialized"
 */
package flexibleconstructors.abstractmethod;